{
  "name": "@ntux402/vendor-aisa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "x402 resource server fronting AIsa's paid search API. Holds AISA_VENDOR_KEY; the orchestrator must never import it.",
  "main": "./src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ntux402/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
