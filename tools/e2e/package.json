{
  "name": "@ntux402/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "M2b operator scripts: run one real goal against Base Sepolia and measure Inco reveal latency. Not part of the trust boundary — plays user, relay and finalizer at once.",
  "scripts": {
    "m2b": "tsx src/m2b-goal-cycle.ts",
    "preflight": "tsx src/preflight.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inco/lightning-js": "^1.0.2",
    "viem": "^2.55.11"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^5.9.2"
  }
}
