{
  "name": "@ntux402/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Operator scripts: run one real goal against Base Sepolia and measure Inco reveal latency. Not part of the trust boundary — plays user, relay and finalizer at once.",
  "scripts": {
    "preflight": "tsx src/preflight.ts",
    "showtime": "tsx src/showtime.ts",
    "typecheck": "tsc --noEmit",
    "state": "tsx src/vault-state.ts",
    "demo": "tsx src/demo.ts",
    "adversary": "tsx src/adversary.ts",
    "tee-check": "tsx src/tee-check.ts",
    "balances": "tsx src/balances.ts",
    "keygen": "tsx src/keygen.ts",
    "fund": "tsx src/fund.ts",
    "whois": "tsx src/whois.ts",
    "record": "tsx src/record-demo.ts"
  },
  "dependencies": {
    "@inco/lightning-js": "^1.0.2",
    "viem": "^2.55.11",
    "@ntux402/shared": "workspace:*",
    "@ntux402/signer": "workspace:*",
    "@ntux402/orchestrator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^5.9.2",
    "playwright": "^1.56.0"
  }
}
